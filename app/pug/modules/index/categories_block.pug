section.categories_block
    .container
        .categories_block__list
            .slider(data-cslider='{"type": "tworow", "initUnder": 767}')
                .slick
                    -for (var i = 1; i < 25; i++)
                        .item: +link('Categories ' + i, '#')
                span.load
